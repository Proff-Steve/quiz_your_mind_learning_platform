Under “Start Learning” create a card called “Make Study Notes”, when users click on it they are taken to the make study note page

Please read the following requirements carefully and build the UI, backend parsing pipelines, and AI integration steps systematically.

### 1. UI WORKSPACE LAYOUT (Three-Pane Split View)
Create a responsive dashboard layout divided into a Left Column (1 Big Pane) and a Right Column (Separated horizontally into 2 Smaller Panes).
- LEFT PANE (Big Area): A fully interactive, full-height Document Editor. It must include complete rich-text editing controls (headings, bold, italics, lists, alignment, and formatting tools) to let users manually adjust text. The content generated by the AI should render directly here in a clean, highly organized format.
- TOP RIGHT PANE (Smaller Panel 1): A file upload and ingestion zone. It should feature a clean drag-and-drop UI. It must support a wide list of 40+ media formats divided into processing categories:
  * Text/Docs: pdf, txt, md, docx, csv, pptx, epub
  * Audio: aac, aif, aifc, aiff, amr, au, m4a, mid, mp3, ogg, opus, ra, ram, snd, wav, wma, cda
  * Video: 3g2, 3gp, avi, mp4, mpeg, Youtube video link
  * Images: avif, bmp, gif, ico, jp2, png, webp, tif, tiff, heic, heif, jpeg, jpg, jpe
- BOTTOM RIGHT PANE (Smaller Panel 2): The prompt input workspace. This contains a text area where users type custom natural language commands for what they want the system to do or generate.

### 2. CORE FUNCTIONALITY & PIPELINES
Implement the backend routes and logic needed to connect these three panes:
- Search & Short-Sentence Expansion: When a user types a specific topic or uploads a PowerPoint slide containing short, concise bullet points, use an LLM to expand that content into highly structured, comprehensive, and exhaustive study notes, if they ask to build on their doc. Look at their doc content and  Pull information from wikipedia together with images to enrich their notes
- Document Summarization: If a user uploads standard documents (PDF, DOCX, TXT, etc.), parse the files, extract the text, and generate a clear, informative study summary, if they request for summary
- Multi-Format Parsing (Backend):
  * For standard documents, use text-extraction libraries to get clean text.
  * For images, provide a basic OCR processing path to grab slide text.
  * For audio/video formats, implement a workflow to transcribe the audio data into a text transcript before feeding it to the AI.
- Wikipedia & Image Enrichment: Implement a specialized system command or trigger. When a user requests it in the prompt area, the app must hit the public Wikipedia API to pull relevant informational summaries AND article image URLs, and inject them directly into the Left Pane text editor seamlessly alongside the notes. 
- When users do not upload anything but request for notes on specific topics, pull that information directly from wikipedia together with images well organized
- If information is pulled from wikipedia, always ensure to list the references as written in wkipedia on the last page

### 3. IMPLEMENTATION WORKFLOW
1. Scan my existing web application to understand the current tech stack, styling frameworks (e.g., Tailwind CSS), and state management.
2. Build the three-pane layout, ensuring it integrates perfectly with our current app theme and navigation.
3. Integrate a robust open-source rich text editor library (such as TipTap, Editor.js, or Quill) into the Left Pane.
4. Build the multi-format file upload handler on the backend to parse the incoming text, images, audio, or video files.
5. Set up the LLM prompt engineering and API logic to handle standard summaries, short-sentence lecture slide expansions, and Wikipedia fetching.

Please write clean, modular code, update the necessary routing and database schemas if required, and provide step-by-step progress as you build this feature.
